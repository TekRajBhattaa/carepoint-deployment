<!--
THIS FILE IS GENERATED CODE!
ALWAYS APPLY CHANGES TO: <PROJECT>/dev/tmpl/symbol/sprite.html.hbs
-->

<style>
  .icon {
    width: 40px;
    height: 40px;
    fill: #000;
  }

  .icon-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(96px, 1fr));
    margin: 0;
    padding: 0;
    list-style: none;
    grid-gap: 20px;
    margin-bottom: 20px;
  }

  .icon-list li {
    position: relative;
    vertical-align: top;
    outline: 1px solid #ccc;
    padding: var(--gu1);
    cursor: default;
  }

  .icon-list .icon-box {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0;
    width: 100%;
    outline: 1px solid #ccc;
    aspect-ratio: 1 / 1;
  }

  .bg-pattern1 {
    background-color: #eee;
    background-image: linear-gradient(45deg, #ccc 25%, transparent 25%, transparent 75%, #ccc 75%, #ccc),
      linear-gradient(45deg, #ccc 25%, transparent 25%, transparent 75%, #ccc 75%, #ccc);
    background-size: calc(var(--checkboard) * 2) calc(var(--checkboard) * 2);
    background-position: 0 0, var(--checkboard) var(--checkboard);
  }

  .bg-pattern2 {
    background-color: var(--cPrimaryContrast);
    background-image: radial-gradient(circle, #ddd 1px, rgba(0, 0, 0, 0) 1px);
    background-size: var(--gu1) var(--gu1);
  }

  .icon-list li h5 {
    font-size: 14px;
    margin: 4px 0;
  }
</style>

<section class="Section Section-space" style="--gu1:8px">
  <!--
  A) Inline SVG with embedded sprite
  ====================================================================================================
  These SVG images make use of fragment identifiers (IDs) and are extracted out of the inline sprite
  embedded above. They may be styled via CSS.
  -->
  <div class="container">
    <h3 class="Hl Hl-3">Inline SVG with embedded sprite</h3>
    <ul class="icon-list">
      {{#shapes}}
      <li title="{{name}}">
        <div class="icon-box bg-pattern2">
          <!-- {{name}} -->
          <svg class="Icon Icon-{{name}}">
            <use href="/ico.svg#{{name}}"></use>
          </svg>
        </div>
        <h5>{{name}}</h5>
      </li>
      {{/shapes}}
    </ul>

    <footer>
      Generated at {{date}} by
      <a href="https://github.com/jkphl/svg-sprite" target="_blank" rel="noopener noreferrer">verdure (svg-sprite)</a>
    </footer>

    <script>
      // Load icons with ajax
      /*
      fetch('icons.svg')
          .then((response) => response.text())
          .then((result) => document.body.insertAdjacentHTML('afterbegin', result));
      */
    </script>
  </div>
</section>
